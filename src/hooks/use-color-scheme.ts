/**
 * Re-exports the native useColorScheme hook from react-native for mobile platforms.
 * (Web platform will use use-color-scheme.web.ts instead due to Metro resolution)
 */
export { useColorScheme } from 'react-native';
